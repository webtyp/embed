# Binaries for programs and plugins
.env
*.exe
*.exe~
*.dll
*.log

# Test binary, built with 'go test -c'
*.test

# Output of the go coverage tool, specifically when used with LiteIDE
coverage.html
*.out

# Dependency directories (remove the comment below to include it)
# vendor/

# Go workspace file
*.code-workspace
.vscode
go.work

# Real model artifacts for TestStaticEmbedder_MatchesReference — 100+ MB, never committed.
# Generate locally with weightsc against the real hotchpotch/bekko-embedding-v1-a8m files;
# see docs/PLAN.md Cambio 5. testdata/reference_vectors.json (the small ground-truth vectors)
# is NOT covered by this and IS committed.
testdata/*.wtypw
testdata/*.merges
